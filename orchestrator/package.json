{
  "name": "@vajra/orchestrator",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "description": "Background coding orchestrator for Linear-driven agent workflows",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "find tests -name '*.test.ts' | xargs tsx --test --test-timeout=60000"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "fastify": "^5.8.2",
    "js-yaml": "^4.1.0",
    "liquidjs": "^10.24.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.2"
  }
}
